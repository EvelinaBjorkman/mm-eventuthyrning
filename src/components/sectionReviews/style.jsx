import styled from 'styled-components';
import { colorPopLight } from '../variables';

export const StyledSectionReviews = styled.section`
  height: 50vh;
  width: 100%;
  background: ${colorPopLight};

`;